# Sample environment file for docker-compose and backend

# Backend DB connection (URL-encoded if special chars in password)
# Example points to the MySQL service name on the compose network
DATABASE_URL=mysql+pymysql://caturro_user:CHANGE_ME_PASSWORD@mysql:3306/caturro_cafe

# Backend security
ADMIN_TOKEN=CHANGE_ME_STRONG_RANDOM_TOKEN
CORS_ORIGINS=http://localhost:3005,http://frontend:3000,https://your-domain.tld

# MySQL service (used by mysql container)
MYSQL_ROOT_PASSWORD=CHANGE_ME_ROOT
MYSQL_DATABASE=caturro_cafe
MYSQL_USER=caturro_user
MYSQL_PASSWORD=CHANGE_ME_DB_PASSWORD

# Frontend (Vite)
VITE_API_URL=http://localhost:8005/api

# Timezone for containers
TZ=America/Bogota

